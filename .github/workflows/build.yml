name: Build APK
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y openjdk-17-jdk zip unzip build-essential git libffi-dev libssl-dev
          pip install buildozer cython==0.29.33 kivy
      - name: Create buildozer.spec
        run: |
          cat > buildozer.spec << 'EOF'
          [app]
          title = Buddy Assistant
          package.name = buddyassistant
          package.domain = org.buddy
          source.dir = .
          source.include_exts = py
          version = 1.0
          requirements = python3,kivy,speechrecognition
          orientation = portrait
          android.permissions = INTERNET,RECORD_AUDIO,MODIFY_AUDIO_SETTINGS
          android.api = 31
          android.minapi = 21
          android.accept_sdk_license = True
          [buildozer]
          log_level = 2
          EOF
      - name: Build APK
        run: yes | buildozer android debug
      - name: Upload APK
        uses: actions/upload-artifact@v3
        with:
          name: BuddyAssistant-APK
          path: bin/*.apk
